require 'spec_helper'

describe Terraforming::Datadog do
  it 'has a version number' do
    expect(Terraforming::Datadog::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
